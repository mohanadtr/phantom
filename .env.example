# Minecraft server connection
SERVER_HOST=localhost
SERVER_PORT=25565

# Bot usernames (comma-separated)
BOT_USERNAMES=DragonSlayer77,ShadowNinjaXD,EnderLegend99

# Reconnect delay in milliseconds (default: 30000)
RECONNECT_DELAY=30000

# Delay between each bot connection in milliseconds (default: 5000)
CONNECTION_DELAY=5000
